/* eslint-disable no-undef */

/* 
Its very unresonable to test for handlers that heavily rely on API calls because of rate limits and stuff
so I won't implement any tests for API.ts.
*/

test('No API.ts tests', () => {
	expect(true).toBe(true);
});